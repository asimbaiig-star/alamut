// Vitest setup — runs before every test file.
//
// `@testing-library/jest-dom` adds DOM-aware matchers like
// `.toBeInTheDocument()`, `.toHaveTextContent(...)`. They're harmless
// in the node-env unit tests (the module just augments expect); the
// component tests need them.
import '@testing-library/jest-dom/vitest';
